void gameScreen() {
  frameRate(60);
  background(0);
  textSize(1000);// this text size is really really really really really big
  color(250);
  text("GO!", 920, 800);
  keyPressed();
  mousePressed();
  display();
  displayball();
}
